<%= form_for handvalve, remote: true do |f| %>
  <%= f.hidden_field :item_received, value: metrics_increment(handvalve.item_received) %>
  <%= f.button icon_select(handvalve.item_received).html_safe, class: 'metrics-button' %>
<% end %>
