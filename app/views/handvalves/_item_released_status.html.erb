<%= form_for handvalve, remote: true do |f| %>
  <%= f.hidden_field :item_inspected_and_released, value: metrics_increment(handvalve.item_inspected_and_released) %>
  <%= f.button icon_select(handvalve.item_inspected_and_released).html_safe, class: 'metrics-button' %>
<% end %>
