<% @handvalve.attributes.each do |name, value| %>
  <% if @handvalve.metrics_attributes.include?(name) %>
    $("#<%= name %>_status_handvalve_<%= @handvalve.id %>").html(
      "<%=
        j render partial: 'shared/metric_status',
          locals: {
          record: @handvalve,
          attribute: name,
          value: value
        }
      %>");
  <% end %>
<% end %>
