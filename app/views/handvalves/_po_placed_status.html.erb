<%= form_for handvalve, remote: true do |f| %>
  <%= f.hidden_field :po_placed, value: metrics_increment(handvalve.po_placed) %>
  <%= f.button icon_select(handvalve.po_placed).html_safe, class: 'metrics-button' %>
<% end %>
