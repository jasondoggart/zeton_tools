<% @instrument.attributes.each do |name, value| %>
  <% if Instrument.metrics_attributes.include?(name) %>
    $("#<%= name %>_status_instrument_<%= @instrument.id %>").html(
      "<%=
        j render partial: 'shared/metric_status',
          locals: {
          record: @instrument,
          attribute: name,
          value: value
        }
      %>");
  <% end %>
<% end %>

