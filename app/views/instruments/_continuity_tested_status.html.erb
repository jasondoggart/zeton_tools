<%= form_for @instrument, remote: true do |f| %>
  <%= f.hidden_field :continuity_tested, value: metrics_increment(@instrument.continuity_tested) %>
  <%= f.button icon_select(@instrument.continuity_tested).html_safe, class: 'metrics-button' %>
<% end %>
