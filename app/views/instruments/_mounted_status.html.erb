<%= form_for instrument, remote: true do |f| %>
  <%= f.hidden_field :mounted_by_mechanical, value: metrics_increment(instrument.mounted_by_mechanical) %>
  <%= f.button icon_select(instrument.mounted_by_mechanical).html_safe, class: 'metrics-button' %>
<% end %>
