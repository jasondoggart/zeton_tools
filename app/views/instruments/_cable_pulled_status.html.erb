<%= form_for @instrument, remote: true do |f| %>
  <%= f.hidden_field :cable_pulled, value: metrics_increment(@instrument.cable_pulled) %>
  <%= f.button icon_select(@instrument.cable_pulled).html_safe, class: 'metrics-button' %>
<% end %>
