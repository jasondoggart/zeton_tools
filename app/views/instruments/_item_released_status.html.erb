<%= form_for instrument, remote: true do |f| %>
  <%= f.hidden_field :item_inspected_and_released, value: metrics_increment(instrument.item_inspected_and_released) %>
  <%= f.button icon_select(instrument.item_inspected_and_released).html_safe, class: 'metrics-button' %>
<% end %>
