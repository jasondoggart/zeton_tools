<%= form_for equipment, remote: true do |f| %>
  <%= f.hidden_field :item_inspected_and_released, value: metrics_increment(equipment.item_inspected_and_released) %>
  <%= f.button icon_select(equipment.item_inspected_and_released).html_safe, class: 'metrics-button' %>
<% end %>
