<%= form_for equipment, remote: true do |f| %>
  <%= f.hidden_field :item_inspected_by_eng, value: metrics_increment(equipment.item_inspected_by_eng) %>
  <%= f.button icon_select(equipment.item_inspected_by_eng).html_safe, class: 'metrics-button' %>
<% end %>
